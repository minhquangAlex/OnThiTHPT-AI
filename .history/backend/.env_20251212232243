# ===============================================================
#         BIẾN MÔI TRƯỜNG CHO BACKEND - OnThiTHPT AI
# ===============================================================

# --- Cấu hình Database MongoDB ---
# Connection string từ MongoDB Atlas
# Chú ý: 
# 1. Thay <username> và <password> bằng thông tin đăng nhập thật
# 2. Thay cluster0.xxxxx bằng tên cluster của bạn từ connection string
# 3. Giữ nguyên /onthithpt_ai ở cuối để chỉ định tên database
MONGO_URI=mongodb+srv://admin:adminpassword@cluster0.qlihfv9.mongodb.net/onthithpt_ai?retryWrites=true&w=majority

# --- Cấu hình Xác thực (Authentication) ---
# Một chuỗi ký tự bí mật, dài và ngẫu nhiên để ký (sign) JWT.
# Thay thế "your_super_secret_jwt_key_here" bằng một chuỗi của riêng bạn.
JWT_SECRET=onthithpt_ai_jwt_secret_key_2025_@#$%^&*()_+

# --- Cấu hình Server ---
# Cổng mà server backend sẽ lắng nghe. Mặc định là 5001.
PORT=5001


# Chế độ chạy của Node.js (development hoặc production)
NODE_ENV=development
